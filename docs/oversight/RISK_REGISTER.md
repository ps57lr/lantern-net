# Lantern risk register

Status: living register; current local checkpoint `0.3.0.dev6`
Review date: 2026-08-17
Companion: `THREAT_MODEL.md`

Likelihood and impact describe plausible risk for the planned product. “Gate” means the capability cannot advance beyond mock/read-only scaffolding until the treatment has independent evidence.

| ID | Risk | Likelihood | Impact | Current evidence/control | Required treatment and verification | Gate/owner | Residual target |
|---|---|---:|---:|---|---|---|---|
| R-001 | Elevating the current UI/Core or generic subprocess runner enables path/environment hijacking and excessive privileged capability. | Medium | Critical | Current collectors run without root; no remediation exists. `run_ok` uses inherited environment/`PATH`. | Separate narrow privileged helper; typed/versioned allowlist; absolute tools/native APIs; sanitized environment; authenticated IPC; reject unknown/extra parameters; no shell/script/URL execution; hostile IPC and local impersonation tests. | Remediation gate; Core/Platform | Low |
| R-002 | A false or stale diagnosis triggers a harmful configuration change. | High | High | Findings are informational today; no action engine. Several checks rely on ambiguous single signals. | Evidence references, confidence, counter-evidence, fresh precondition immediately before apply, no automatic execution, per-action approval, fault fixtures, state-race tests. | Remediation gate; Diagnosis | Low–Medium |
| R-003 | Partial/interrupted remediation leaves connectivity or system state worse, and advertised rollback is incomplete. | Medium | Critical | No remediation implementation. | Explicit lifecycle and transaction journal; idempotence; safe cancellation points; verify; tested rollback and rollback-failure recovery; never claim rollback unless proven. | Remediation gate; Platform | Low–Medium |
| R-004 | A LAN peer pairs without authorization or reuses/stole a session. | High | Critical | LAN responder does not exist. | Authenticated encrypted key establishment bound to locally displayed high-entropy code/QR and device identity; rate limits; replay protection; short expiry; rotation; local visibility and revocation; pairing/session attack tests. | LAN gate; Session/Security | Low |
| R-005 | LAN service binds wildcard, VPN, public, container, or unintended IPv6 interface and becomes externally reachable. | Medium | Critical | v0.2.1 scopes LAN collection to the default interface, but no listener exists. | Bind exact selected address; fail closed on ambiguity; no UPnP/firewall/NAT changes; enumerate sockets in tests; VPN/multihome/IPv6/container fixtures; clean shutdown. | LAN gate; Network | Low |
| R-006 | Browser attacks (CSRF, malicious Origin, WebSocket hijack, XSS, session fixation, request smuggling) invoke diagnostics or actions. | High | High | Local UI binds literal loopback with a per-launch hostname/capability, strict Host/Origin/CSRF, same-site HttpOnly session, CSP, exact local assets, bounded JSON, safe DOM construction, expiry/revoke, and adversarial browser tests. No action endpoint exists. | Preserve controls in installed-artifact tests; independently redesign identity and transport before LAN or privilege; replace the accepted final-auth-to-response-write race before a higher-trust surface. | UI and LAN gates; UI/Security | Low |
| R-007 | Share-safe reports leak identifiers through derived prose, errors, raw native output, public/LAN IPs, or new unclassified fields. | High | High | Typed structural redaction and fail-closed serializers cover reports; the local UI exports only its bounded identifier-free presentation after preview. No export endpoint or raw evidence is exposed to JavaScript. | Keep canary/Unicode/nested-error tests; preview and call exports share-safe rather than anonymous; separately review any future raw report, plugin, upload, or LAN transport. | Export/LAN gate; Core/Privacy | Low–Medium |
| R-008 | Passwords, tokens, pairing codes, router credentials, or recovery keys enter UI state, logs, reports, crash data, or audit records. | Medium | Critical | Current tool collects no credentials. | Declarative access-prerequisite model; OS/device-owned prompts; no generic credential forms; secret types forbidden in report schema; memory-only session secrets; log filters/canaries; no telemetry by default. | Credential integration gate; Security/UI | Low |
| R-009 | Rescue mode writes to the wrong/suspect disk, weakens encryption/boot security, or causes data loss. | Medium | Critical | Rescue mode does not exist. | Separate rescue runtime; read-only default; exact device identity; physical-presence consent; power checks; never bypass Secure Boot/FileVault/BitLocker; red-tier writes; damaged/encrypted/multidisk fixtures and platform review. | Rescue gate; Recovery | Low–Medium |
| R-010 | Tampered USB, writable-path dependency loading, or artifact substitution executes attacker code. | Medium | Critical | A locked, deterministic unsigned development pipeline and clean-profile verifier exist. The immutable dev4 artifact passed clean build, Developer ID signing, Apple notarization, stapling, and local verification, but exposed a Safari foreground usability defect. The untagged, non-distributed dev5 candidate fixed foregrounding, but signed Finder low-impact smoke exposed inherited multiline gateway-ping serialization failure. No updater exists, and signing does not make copied files on writable media tamper-resistant. | Dev6 must independently repeat the complete clean-build, signing, notarization, stapling, verification, Gatekeeper, Finder-opening, and low-impact smoke gates. Preserve reproducible build records, hashes, dependency inventory, protected launcher/library search paths, version identity, clean-machine validation, and explicit unsigned warnings. Never imply a hash is publisher identity. | Packaging gate; Release | Low–Medium |
| R-011 | Automatic scanning probes an unauthorized subnet/device or disrupts a fragile network. | Medium | High | Ping sweep is bounded/scoped and explicit, but full scan automatically browses mDNS and attempts gateway ports. | Per-probe impact registry; visible target/interface/CIDR; separate active consent; packet/concurrency budgets; default safe profile; no broad scan during unattended UX test; scope and rate tests. | UI/LAN gate; Network/Product | Low |
| R-012 | Unsupported, denied, timed-out, skipped, empty, or crashed checks are summarized as healthy. | High | High | Report 1.1 separates execution, outcome, severity, and coverage; the UI viewmodel derives and cross-validates child/aggregate states and cannot report live complete health when current profiles omit work. | Preserve typed states, coverage counts, nontruncated aggregate checks, and impossible-health adversarial fixtures across CLI, UI, and installed artifacts. | Core gate; Core/UI | Low |
| R-013 | Native-command errors/localized or hostile output are parsed as valid evidence or injected into the UI. | High | Medium | `run_ok` merges stderr/stdout, ignores nonzero status, and inherits locale. Parsers are regex-based. | Structured command result with exit status and separate streams; stable locale where safe; absolute size limits; parser fixtures across OS/locales; fuzz/hostile-string tests; HTML escaping. | Core/UI gate; Platform/UI | Low–Medium |
| R-014 | Hung probes, thread pools, malformed routing buffers, or LAN requests exhaust time, memory, descriptors, or packets. | Medium | High | Collectors and local requests have bounded outputs/deadlines; status SSE is limited to one stream per session, four globally, and 25 seconds with deterministic lease release and polling fallback. | Retain global cancellation, per-probe budgets, bounded output/concurrency, cancellation-safe process handling, repeated-run and descriptor/stream-leak tests; set separate LAN budgets before any listener. | Core/LAN gate; Core/Network | Low |
| R-015 | A route lookup outside scan isolation aborts the full report, losing evidence and presenting a poor first-use failure. | Medium | Medium | Route collection is an isolated typed check; command failure produces an inconclusive/check-failed result while other evidence remains available. | Preserve route command-failure, timeout, cancellation, and partial-report regression fixtures. | Core correctness; Core | Low |
| R-016 | Default gateway is conflated with default route, causing false critical diagnosis on VPN/PPP/point-to-point systems. | Medium | Medium | Route existence is separate from an explicit next-hop address; missing/unavailable IPv4 observation is inconclusive, and local-route success does not claim Internet/DNS success. | Preserve VPN/PPP/on-link/IPv6-not-tested fixtures and never authorize remediation from gateway absence alone. | Core correctness; Platform/Diagnosis | Low |
| R-017 | UI complexity, inaccessible controls, color-only severity, or misleading errors causes unsafe consent or repair decisions. | High | High | Responsive local UI has a shared status vocabulary, progressive disclosure, non-color states, explicit consent, keyboard/mobile drawer controls, reduced-motion/forced-color support, axe/Playwright coverage, and truthful partial/error views. Repairs remain disabled. | Continue real-browser assistive-technology and 200% zoom review; rerun source and installed-artifact accessibility matrices for each release; separately test any future repair consent. | Local UI gate; Product/UI | Low |
| R-018 | Technician UI claims it inspected endpoint internals using only network observation. | Medium | High | The local UI explicitly reports one endpoint vantage point; no LAN/technician UI or endpoint pairing is enabled. | Preserve the capability matrix and evidence-source labels; require a “network view only” banner, negative scenarios, and documentation review before any LAN technician surface. | LAN gate; Product/Diagnosis | Low |
| R-019 | Audit data is sensitive, mutable, or ambiguous about actor, approval, action, and result. | Medium | High | Typed, fail-closed in-memory action attempts and sealed LAN audit-event foundations exist, but no concrete remediation or persistent operational journal is enabled. | Before execution, add an integrity-protected lifecycle journal, actor/approval/action/result binding, retention/deletion, redacted export, crash recovery, and failure-path review. | Remediation/LAN gate; Core/Security | Low–Medium |
| R-020 | A local malicious process impersonates the UI/helper or steals local loopback/session capabilities. | Medium | High | The local UI uses an unguessable one-use fragment, host-only cookie, unique hostname, strict origin, short expiry, and revoke; no helper or privileged API exists. A malicious same-user process or browser extension remains in the threat model. | Add OS-protected IPC/peer identity before privilege; preserve short lifetime and local-adversary tests; do not treat loopback as an authorization boundary for remediation. | UI/remediation gate; Security/Platform | Low–Medium |
| R-021 | Portable build silently auto-runs or trains users to approve an unexpected elevation prompt, resembling malware behavior. | Medium | High | The packaging foundation creates an obvious user-launched app with no autorun, installer, persistence, or elevation, a fixed safe startup failure, and explicit trust labeling. The immutable signed, notarized, and stapled dev4 family beta passed local verification but can open Safari behind other windows. The untagged dev5 candidate fixed foregrounding but was rejected during low-impact smoke; dev6 combines that fix with safe canonical ping serialization without adding those capabilities. | Before dev6 handoff, independently repeat clean build, signed publisher identity verification, Apple notarization, stapling, Gatekeeper assessment, nontechnical Finder-opening tests, and low-impact smoke. Keep no-autorun and no-persistence, and require just-in-time explanation before any future elevation. | Packaging/UI gate; Product/Release | Low |
| R-022 | Updater compromise installs privileged malicious code or permits downgrade. | Medium | Critical | No updater exists. | Do not enable yet. Future signed metadata/artifacts, threshold/offline keys, rollback protection, expiry, atomic install, channel separation, revocation/compromise plan and negative tests. | Future update gate; Release/Security | Low |
| R-023 | Findings drift across platform releases, brief mDNS/neighbor windows, or missing tools and create inconsistent triage. | High | Medium | Real Mac/Pi validation and parser tests exist; sample set is small. | Versioned real fixtures, provenance/tool versions, confidence/coverage, repeated-window semantics, supported-platform matrix, false-positive incident corpus. | Ongoing; Platform/Diagnosis | Medium |
| R-024 | Rescue or portable UI asks for credentials/recovery keys in a Lantern-owned form and creates a phishing pattern. | Medium | Critical | No such UI exists. | Access requirements only; direct OS/device prompt; strong contextual copy; never request a recovery key unless the platform recovery surface owns input; UX/security test. | UI/Rescue gate; UI/Security | Low |
| R-025 | A “first-time user” unattended test mutates the real Mac/network, exposes a service, or records private household data. | Medium | High | User authorized build/test, not real configuration changes or credential use. | Clean profile; read-only/default-redacted mode; mocks for fixes; loopback only; no credentials/elevation/broad discovery; local artifacts kept out of Git; review and delete test reports. | Immediate; Lead/Oversight | Low |
| R-026 | A stale, expired, or excessive status stream leaks updates, exhausts worker threads, or leaves the browser frozen after its stream lifetime. | Medium | Medium | Polling and each stream re-authenticate after provider access; streams use one-per-session/four-global leases, close after 25 seconds, release on terminal/error/disconnect, and the client polls after close before reopening. | Preserve expiry/revoke/blocking-provider/BrokenPipe/429/stream-limit browser canaries. Eliminate the tiny final-auth-to-response-write race before LAN, credentials, or privilege. | Local UI gate; UI/Security | Low |
| R-027 | A locally downloaded share-safe file is mistaken for anonymous data or is retained/synced beyond the user's intent. | Medium | High | The UI previews exact validated presentation JSON, explains included/excluded content and residual timing/diagnostic disclosure, requires an explicit download, and performs no upload. Browser/OS/backups may retain or sync the file. | Keep preview/download deep-equality and secret/IP/MAC canaries; never claim anonymity or control of destination retention; design managed retention/deletion only with a separately reviewed storage service. | Export gate; Privacy/Product | Low–Medium |

## Immediate conditions and ownership

1. **Program lead:** keep implementation inside the dedicated worktree; preserve v0.2.1 as the comparison point; record scope and clean validation evidence.
2. **Core/Platform:** preserve the implemented R-012, R-015, and R-016 controls; replace the legacy generic `run_ok` boundary in R-013 before packaged elevated or production use.
3. **Core/Privacy:** build sensitivity-aware evidence/report contracts and canary tests before automatic report transport.
4. **Product/UI:** produce consent, state, risk, accessibility, responsive technician, and rescue interaction specifications; show unsupported/denied/inconclusive states honestly.
5. **Security/Session:** complete LAN and local-web ADR/threat update before opening a non-loopback port.
6. **Remediation:** remain dry-run/mock only until R-001 through R-003 and R-019 have executable test evidence.
7. **Recovery:** remain guidance/read-only only until R-009 and R-024 receive platform-specific review.
8. **Release:** do not push, tag, sign, notarize, publish, or enable updates without explicit user authorization.

## Phase verdict criteria

- **PASS:** all risks gating that phase have implemented controls and reviewed evidence; residual risks are accurately documented.
- **PASS WITH CONDITIONS:** safe work may continue, but named controls must land before the next trust boundary is crossed.
- **BLOCK:** a critical boundary is crossed without its controls, tests are weakened to fit behavior, credentials/secrets enter artifacts, real mutation occurs without approval, or the UI materially overstates coverage/safety.

Current verdict: **PASS WITH CONDITIONS** for the disconnected, unprivileged local development preview and read-only foundations through Phase 2.2. Remediation execution, LAN exposure, credential handling, rescue writes, unsigned artifact distribution, and production claims remain blocked behind their separate gates.
